# Leia-me se for o Diogo - by: Wagner

**Pacotes que estamos usando (NPM):**
express, cors, dotenv, mysql2, typeorm, reflect-metadata, typescript, ts-node-dev, @types/node, @types/express, @types/cors

---

Diogo, resumindo rápido:

Já deixei o backend todo configurado e criei todos os models do banco no TypeORM, já certinhos com os relacionamentos, pra facilitar nossa vida e evitar ficar fazendo código Frankenstein.

O módulo de aluno já está completo no backend (CRUD funcionando e testado). Falta só fazer o fetch no frontend, mas vou deixar isso pra depois, quando tudo estiver pronto.


A base já tá pronta, agora é só continuar em cima.
